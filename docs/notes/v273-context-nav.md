# EVO V2.7.3 contextual navigation

The contextual back-to-top control replaces any stale DOM instance created by an older cached loader before installing the current My EVO / public Passport behavior.
