# Contracts

No production EVO Protocol contract is deployed from this repository yet.

The first proposed contract will be a minimal `EVOSealRegistry` developed and tested on Polygon testnet before any mainnet deployment. The registry should avoid storing large/private content and should not custody unrelated user funds.
